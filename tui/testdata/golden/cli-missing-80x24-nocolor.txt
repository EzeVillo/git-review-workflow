The git-review CLI (0.8.0 or newer) was not found.
Install with npm (recommended):

  npm install -g git-review-workflow

Press r to refresh, or focus this pane again — the panel does not poll.

[ Other install options ]

exec: "git-review": executable file not found in $PATH

j:up  k:down  enter:select  r:refresh  ::actions  m:mouse on  q:quit