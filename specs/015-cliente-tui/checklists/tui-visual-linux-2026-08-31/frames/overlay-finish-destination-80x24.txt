Where should your edits go?

> A separate branch
    review-fixes/<branch>, staged on top of the PR tip
  Onto the PR branch itself
    stage the edits directly on the PR branch

j/k:move  enter:pick  esc:cancel
















